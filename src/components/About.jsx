import styles, { layout } from "../style";

const About = () => (
  <section
    className={`${styles.flexCenter} flex-row flex-wrap sm:mb-20 mb-6 border`}
    id="About"
  >
    <p className="text-white">About us</p>
  </section>
);

export default About;
