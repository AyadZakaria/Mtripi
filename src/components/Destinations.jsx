import styles from "../style";

const Destinations = () => (
  <section className={`${styles.flexCenter} flex-row flex-wrap sm:mb-20 mb-6 border`} id="destination">
   <p className="text-white">Destinations </p>
   
  </section>
);

export default Destinations;
